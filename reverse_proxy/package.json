{
  "private": true,
  "name": "proxy",
  "version": "0.0.1",
  "description": "Cloudflare reverse proxy with middleware to set headers",
  "main": "worker/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Fionn",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
